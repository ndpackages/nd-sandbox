const socketEventEnum = Object.freeze({

    OPEN: 'socket.open',
    CLOSE: 'socket.close',
    BREAK: 'socket.break',
    MESSAGE: 'socket.message',
    ERROR: 'socket.error',

});

export default socketEventEnum;
